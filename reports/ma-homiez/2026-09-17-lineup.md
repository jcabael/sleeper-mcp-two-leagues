# Ma Homiez — Weekly Lineup Cycle — 2026-09-17

**Status: BLOCKED — no lineup verdict issued.**

League: Ma Homiez (Sleeper) · `league_id` 1389709287655235584 · Jon's `roster_id` 4. Settings on file per `CLAUDE.md`: 12-team keeper/dynasty-adjacent, superflex, 2× IDP_FLEX, half-PPR (WR/TE 1.0 pt/rec, RB 0.5 pt/rec), passing TD +6/INT −4, IDP sack +2/QB hit +1/TFL +1/solo tackle +1/multi-sack +2/forced fumble +3.

This run could not produce a real, data-backed lineup recommendation. Per CLAUDE.md rule 1 ("verify settings, don't assume") and rule 6 ("leagues never blend"), no roster, research, or verdict is fabricated below — this report documents the blockers only, for someone to fix before the next run.

## What was attempted

1. **jono** subagent invoked to orchestrate the full cycle (roster pull → matchup-researcher, team-vs-team-researcher, news-sentiment-researcher, waiver-wire-scout → start-sit-optimizer → cross-checker → verdict). It hit a hard stop at the roster-pull step and correctly declined to fabricate downstream output. Its full report is summarized below and was independently verified.

2. **`sleeper-mahomiez` MCP server, roster/league-info tools** — checked directly. Despite the server's name, its tool docstrings show it is hardcoded to a different league entirely:
   - `get_league_info` description: *"comprehensive information about the **Token Bowl** fantasy football league... League ID: Configured via `SLEEPER_LEAGUE_ID` env var (default: **1266471057523490816**)"* — not Ma Homiez's `1389709287655235584`.
   - `get_league_users` / `get_roster` docstrings likewise reference "Token Bowl" and a 10-roster league (`get_roster` notes "Roster ID 2 is Bill Beliclaude" — not a Ma Homiez owner name).
   - Per CLAUDE.md rule 6 ("leagues never blend"), this data source was **not used** to answer the Ma Homiez request — pulling it would silently substitute the wrong league's roster.

3. **Raw Sleeper API fallback** (per CLAUDE.md's Data Access table, `GET https://api.sleeper.app/v1/league/1389709287655235584/...`) — attempted via direct `curl`. Failed with `CONNECT tunnel failed, response 403`. Checked the egress proxy status endpoint directly: this is an **organization policy denial** (`connect_rejected`, "gateway answered 403 to CONNECT (policy denial or upstream failure)"), not a transient idle-server timeout. The same policy also blocks general web access (`www.espn.com`, `www.google.com` both denied identically), which would have blocked the four research specialists' web tools as well even if the roster had been available.
   - Per CLAUDE.md rule 4, the "retry once, then fall back" instruction is for Render-hosted MCP idle spin-down, not for a hard policy denial — retrying would not have changed the outcome, and the repo's own proxy guidance says to report policy denials rather than retry them.

4. **Local cache/fixture check** — searched the repo for any usable snapshot. Found only `data/week3_data.json`, which is fixture data for the unrelated "Token Bowl" league — not substituted for the same reason as (2).

## Specialists / optimizer / cross-checker

Not invoked. matchup-researcher, team-vs-team-researcher, news-sentiment-researcher, and waiver-wire-scout all depend on web access, which is blocked by the same policy denial. start-sit-optimizer requires a real roster and real specialist research, neither of which exists this run. cross-checker requires specialist output to audit — there is none, so it was not invoked (nothing to check, not skipped by choice).

## What needs to change before the next run

1. Fix the `sleeper-mahomiez` MCP server's league binding — it is currently pointed at "Token Bowl" (`league_id` 1266471057523490816) instead of Ma Homiez (`league_id` 1389709287655235584). Likely a `SLEEPER_LEAGUE_ID` env var misconfiguration on the MCP host.
2. Confirm whether `api.sleeper.app` and general web egress are supposed to be reachable from this environment for the scheduled routine; currently both are denied at the organization proxy level.
3. Re-run the full weekly lineup cycle once either the MCP server is repointed or raw API/web egress is restored.

## Fallback offered

If lineups lock before this is fixed, Jon can supply last week's starters and any injury news he's already seen; that can be sanity-checked against roster-slot/IDP/superflex rules as a labeled fallback, not a researched recommendation. Not exercised in this automated run since no live user input is available.
